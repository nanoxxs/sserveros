# sserveros 敏感变量示例
# 复制为 .env 并填写实际值：cp .env.example .env

# ── 推送方式一：Server Chan ────────────────────────────────────────────────────
# 支持多个密钥（逗号分隔），推送时会同时发送到所有密钥
# 获取地址：https://sct.ftqq.com/
# SERVERCHAN_KEYS=SCTkey1,SCTkey2

# ── 推送方式二：Bark ──────────────────────────────────────────────────────────
# 支持多个 Bark 地址，格式：URL|设备Key，多个用逗号分隔
# Bark 官方服务地址：https://api.day.app
# BARK_CONFIGS=https://api.day.app|YourDeviceKey1,https://api.day.app|YourDeviceKey2

# ── 旧版单密钥写法（仍然有效）────────────────────────────────────────────────
# SENDKEY=SCTxxxxxxxxxxxxxxxx

# ── WebUI 初始密码 ─────────────────────────────────────────────────────────────
# 首次自动生成 config.json 时使用（可选）
# SSERVEROS_PASSWORD=
